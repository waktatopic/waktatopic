{
	"name": "waktatopic",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"client-install": "cd client && npm install",
		"server-install": "cd server && npm install",
		"install-all-deps": "concurrently \"npm run server-install\" \"npm run server-install\"",
		"start": "npm cd server && server.js",
		"dev": "nodemon cd server && server.js"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^3.0.1"
	}
}
